spring:
  application:
    name: alibaba-nacos-test
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
#        prefix: demo
        file-extension: yml
#        group: DEFAULT_GROUP


server:
  port: 8005

